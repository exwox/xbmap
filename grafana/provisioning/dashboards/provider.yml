apiVersion: 1

providers:
  - name: liquidmap
    orgId: 1
    folder: LiquidMap
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
